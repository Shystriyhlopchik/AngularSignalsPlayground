# TASK: computed-фильтрация списка

## Задача

Задача: show = signal(false), count = signal(0).
message = computed(() => show() ? \count=${count()}` : 'hidden')`

Готово, когда:
при show=false изменение count не приводит к пересчёту message.

## 📌 Контекст

В Angular Signals зависимости `computed` **не фиксируются заранее**.
Отслеживаются **только те сигналы, которые реально были прочитаны** во время последнего вычисления (`derivation`).

Это означает, что зависимости могут:
- **добавляться** (если ветка кода прочитала новый сигнал),
- **удаляться** (если в другой ветке этот сигнал больше не читается).

Задача демонстрирует это на простом примере: `computed` читает `count()` только когда `showCount()` равно `true`.

---

## 🎯 Цель

После выполнения задачи я должен(на) уметь:

- объяснить, что зависимости computed **динамические**
- показать на примере, что сигнал может **перестать быть зависимостью**
- понимать, почему изменение `count` иногда **не вызывает** пересчёт computed
- уметь проверять поведение через `effect` / логи

---

## 🧠 Используемые концепции

- `signal`
- `computed`
- `effect`
- мемоизация (cache)
- динамические зависимости computed

---

## 🛠️ Шаги реализации

1. Создать сигналы:
  - `showCount = signal(false)`
  - `count = signal(0)`

2. Создать computed:
  - `message = computed(() => { ... })`
  - **читать `count()` только внутри ветки `if (showCount())`**

3. Добавить `effect`, который:
  - читает `message()`
  - пишет в консоль текущее значение и количество пересчётов

4. Сделать UI-кнопки:
  - Toggle `showCount`
  - Increment `count`
  - Reset `count`

5. Проверить поведение в трёх сценариях (см. ниже).

## 🔍 Что проверить вручную

### Сценарий A — `showCount = false`
1) Убедиться, что `showCount` выключен
2) Несколько раз нажать `count++`

Ожидаемое:
- текст `message()` остаётся “ничего показывать”
- счётчик пересчётов `message` **не растёт** от изменения `count`

### Сценарий B — переключить `showCount = true`
1) Включить `showCount`
2) Прочитать `message()` (через UI или effect)

Ожидаемое:
- `message()` теперь содержит значение `count`
- `count` стал зависимостью

### Сценарий C — `showCount = true`, меняем `count`
1) Несколько раз нажать `count++`

Ожидаемое:
- `message()` обновляется
- счётчик пересчётов растёт при каждом изменении `count`

### Сценарий D — вернуть `showCount = false`
1) Выключить `showCount`
2) Нажать `count++`

Ожидаемое:
- `message()` снова “ничего показывать”
- `count` больше не зависимость → пересчётов нет

---

## 📚 Связанные материалы


- https://angular.dev/guide/signals
- https://angular.dev/guide/signals/computed
- Раздел: “Computed signal dependencies are dynamic”
  https://angular.dev/guide/signals#computed-signal-dependencies-are-dynamic

---

## 📝 Итоги

После выполнения задачи стало понятно, что:

- зависимости computed формируются **фактом чтения сигналов**
- зависимости могут **добавляться и удаляться** в зависимости от веток выполнения
- это позволяет экономить вычисления и избегать лишних пересчётов

