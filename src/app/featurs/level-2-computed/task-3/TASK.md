# TASK: computed-фильтрация списка

## 📌 Контекст

Задача: items = signal<Item[]>(...), query = signal('').
filtered = computed(() => ...) фильтрует список по query.
Сделай список из 500–2000 элементов, чтобы почувствовать “ленивость/мемоизацию”.

Готово, когда:
filtered() пересчитывается только при изменении items или query.

---

## 🎯 Цель

После выполнения задачи я должен(на) уметь:
- использовать `computed` для фильтрации данных

---

## 🧠 Используемые концепции

- `signal`
- `computed`
- `effect`
- мемоизация (cache)

---

## 🛠️ Шаги реализации

1. Создать тип `Item { id, title }`
2. Сгенерировать массив из ~1500 элементов
3. Объявить сигналы:
  - `items: signal<Item[]>`
  - `query: signal<string>`
4. Реализовать `filtered = computed(() => ...)`, который:
  - читает `items()` и `query()`
  - при пустом `query` возвращает `items()`
  - иначе фильтрует список
5. Добавить `effect`, который:
  - читает `filtered()`
  - увеличивает счётчик пересчётов
6. Реализовать кнопки:
  - изменение `query`
  - замена `items`
  - многократное чтение `filtered()`

## 🔍 Что проверить вручную

- Ввод текста в `query`
- Очистка `query`
- Замена `items` на новый массив с тем же содержимым
- Многократное чтение `filtered()` подряд

---

## 📚 Связанные материалы

- Angular Signals Guide  
- https://angular.dev/guide/signals
- https://angular.dev/guide/signals/computed
- https://angular.dev/guide/signals#computed-signal-dependencies-are-dynamic

---

## 📝 Итоги

Что я понял(а) после выполнения задачи:

- computed выполняется только при его чтении/запросе
- Если зависимости не менялись,
  то повторное чтение computed()
  возвращает сохранённый результат,
  а функция не выполняется снова.

