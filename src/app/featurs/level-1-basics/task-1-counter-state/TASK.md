# TASK: CounterState с readonly сигналом
## Счётчик с публичным readonly API

Задача: сделай CounterState сервис:
внутри приватный _count = signal(0)
наружу count = _count.asReadonly()
методы inc(), dec(), reset() через .update()/.set() Готово, когда:
компонент может читать count() но не может вызвать set/update напрямую.

## 📌 Контекст

В Angular Signals рекомендуется скрывать writable-сигналы внутри сервиса  
и отдавать наружу только readonly API.  
Это предотвращает неконтролируемые изменения состояния из компонентов.

Задача демонстрирует:
- базовую работу с `signal`
- различие между writable и readonly сигналами
- правильное проектирование state-сервиса

---

## 🎯 Цель

После выполнения задачи я должен(на) уметь:
- создавать writable-сигнал внутри сервиса
- публиковать readonly-сигнал наружу
- изменять состояние **только через методы сервиса**
- понимать, почему readonly не защищает от deep-mutation

---

## 🧠 Используемые концепции

- `signal`
- `asReadonly`
- `set`
- `update`

---

## 📋 Условия задачи

- [x] Использовать Angular Signals API
- [x] Компоненты — standalone
- [x] Writable-сигнал скрыт
- [x] Нет прямого доступа к `.set()` из компонента
- [x] Минимальный UI

---

## 🛠️ Шаги реализации

1. Создать сервис `CounterState`
2. Объявить приватный сигнал `_count = signal(0)`
3. Экспортировать `count = _count.asReadonly()`
4. Реализовать методы:
  - `increment()`
  - `decrement()`
  - `reset()`
5. Использовать сервис в компоненте

---

## ✅ Критерии готовности

Задача считается выполненной, если:

- [x] Компонент читает значение через `count()`
- [x] Попытка вызвать `count.set()` невозможна
- [x] Изменение состояния происходит только через сервис
- [x] UI обновляется реактивно

---

## ⚠️ Подводные камни

- `asReadonly()` **не делает глубокую иммутабельность**
- Объекты и массивы внутри сигнала всё ещё можно мутировать
- Это архитектурная защита, а не техническая

---

## 🔍 Что проверить вручную

- Многократное увеличение/уменьшение счётчика
- Сброс состояния
- Поведение после перезагрузки компонента

---

## 🧪 Самопроверка

- Почему `_count` должен быть приватным?
- Что произойдёт, если вернуть writable-сигнал наружу?
- Чем `update()` предпочтительнее прямого `set(count()+1)`?

---

## 📚 Связанные материалы

- https://angular.dev/guide/signals
- https://angular.dev/guide/signals#updating-signals

---

## 📝 Итоги

- Signals хорошо подходят для локального state
- Readonly API — архитектурный приём
- State должен изменяться в одном месте
